{
  "name": "tn170-attendance",
  "private": true,
  "type": "module",
  "scripts": {
    "seed:members": "node scripts/seed-members.js",
    "clear:pins": "node scripts/clear-all-pins.js",
    "build:app": "npm run build --prefix attendance-tracker",
    "build:pages": "npm run build:app && node scripts/sync-github-pages.js",
    "deploy": "npm run deploy:spark",
    "deploy:spark": "npm run build:app && firebase deploy --only firestore:rules,firestore:indexes,hosting",
    "deploy:hosting": "npm run build:app && firebase deploy --only hosting",
    "deploy:functions": "node -e \"console.error('Cloud Functions require Firebase Blaze billing. TN-170 is Spark-only, so this deploy is blocked.'); process.exit(1)\"",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
